@startuml
start
:Получить данные о продукте;
:Получить продажи, остатки и рыночные цены;
:Проверить качество данных;
if (Данные корректны?) then (да)
  :Сформировать признаки;
  :Рассчитать прогноз спроса;
  :Рассчитать рекомендованную цену;
  :Проверить маржинальность;
  if (Цена соответствует ограничениям?) then (да)
    :Сформировать объяснение решения;
    :Создать заявку на согласование;
    :Бизнес-эксперт проверяет цену;
    if (Цена согласована?) then (да)
      :Опубликовать цену;
      :Записать версию расчёта;
      :Сформировать отчёт;
    else (нет)
      :Вернуть на корректировку;
    endif
  else (нет)
    :Пересчитать цену с учётом ограничений;
  endif
else (нет)
  :Вернуть данные на доработку;
endif
stop
@enduml